\begin{figure}
  \centering
  \begin{adjustbox}{width=0.94\textwidth, center}
    \begin{tikzpicture}[node distance=1.25cm and 1.25cm, font=\small]
      \node (state) [pipelinebox, text width=6.8cm] {\textbf{State $s$}\\query + BM25 confidence};
      \node (table) [pipelinebox, text width=6.8cm, right=of state] {\textbf{Full-info replay table}\\all candidate action rewards};
      \node (policy) [policybox, text width=6.8cm, right=of table] {\textbf{Policy $\pi(s)$}\\argmax predicted reward};

      \node (action) [policybox, text width=6.8cm, below=of state] {\textbf{Action $a$}\\BM25, dense, hybrid, rewrite};
      \node (reward) [evidencebox, text width=6.8cm, right=of action] {\textbf{Reward table}\\Recall@5 + 0.5 MRR - cost};
      \node (audit) [evidencebox, text width=6.8cm, right=of reward] {\textbf{Evidence}\\CIs, OPE, bandit replay};

      \draw [arrow] (state.east) -- (table.west);
      \draw [arrow] (table.east) -- (policy.west);
      \draw [arrow] (policy.south) -- ++(0,-0.45) -| (action.north);
      \draw [arrow] (action.east) -- (reward.west);
      \draw [arrow] (reward.east) -- (audit.west);
    \end{tikzpicture}
  \end{adjustbox}
  \caption{Offline contextual-bandit framing: replayable action rewards train a policy that maps query state to retrieval action.}
\end{figure}
