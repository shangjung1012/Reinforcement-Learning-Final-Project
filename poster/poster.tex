% Selective RAG RL final project poster.

\documentclass[final,20pt]{beamer}

% ====================
% Packages
% ====================

\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[orientation=portrait,size=a0,scale=1.08]{beamerposter}
\usetheme{gemini}
\usecolortheme{nott}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.14}
\usepackage{anyfontsize}
\usepackage{xcolor}
\usepackage[skip=2pt,font=large]{subcaption}
\usepackage{adjustbox}
\usepackage{amsmath}
\usepackage{fontspec}

\usetikzlibrary{shapes.geometric, arrows.meta, positioning}

\newfontfamily\cjkfont{Noto Sans CJK TC}

\tikzstyle{pipelinebox} = [rectangle, rounded corners, minimum width=5.5cm, minimum height=1.35cm, text centered, text width=10.5cm, draw=black, fill=white]
\tikzstyle{policybox} = [rectangle, rounded corners, minimum width=5.5cm, minimum height=1.35cm, text centered, text width=10.5cm, draw=black, fill=blue!7]
\tikzstyle{evidencebox} = [rectangle, rounded corners, minimum width=5.5cm, minimum height=1.35cm, text centered, text width=10.5cm, draw=black, fill=green!7]
\tikzstyle{arrow} = [ultra thick,->,>=Stealth]

% ====================
% Lengths
% ====================

\newlength{\sepwidth}
\newlength{\colwidth}
\setlength{\sepwidth}{0.025\paperwidth}
\setlength{\colwidth}{0.45\paperwidth}

\newcommand{\separatorcolumn}{\begin{column}{\sepwidth}\end{column}}

% ====================
% Title
% ====================

\title{Selective and Cost-Aware Retrieval Routing for RAG}

\author{\texorpdfstring{{\cjkfont 蔡尚融}}{蔡尚融} \and \texorpdfstring{{\cjkfont 李羿昕}}{李羿昕} \and \texorpdfstring{{\cjkfont 李政緯}}{李政緯}}

\institute[shortinst]{National Yang Ming Chiao Tung University}

% ====================
% Footer
% ====================

\footercontent{
  \textbf{Code-backed retrieval-stage contextual bandits} \hfill
  \textbf{NYCU 535514 Reinforcement Learning} \hfill
  \textbf{Evidence: outputs/results/final\_claims\_matrix.csv}}

% ====================
% Logo
% ====================

\logoleft{\includegraphics[height=7cm]{logos/NYCU_Logo_C.png}}

% ====================
% Body
% ====================

\begin{document}

\begin{frame}[t]
\begin{columns}[t]
\separatorcolumn

\begin{column}{\colwidth}

  \begin{block}{Abstract}
    This project studies \textbf{retrieval-stage control for RAG} as an offline contextual bandit. For each query, a lightweight policy observes lexical and retrieval-confidence features, then chooses a retrieval or rewrite action. The final claim is intentionally narrow: improved \textbf{cost-aware evidence retrieval}, not generated-answer EM/F1 and not LLM fine-tuning.
  \end{block}

  \begin{alertblock}{Problem and Objective}
    Fixed RAG retrieval cannot adapt to query difficulty. Rewrites, dense retrieval, hybrid retrieval, or LLM experts can help, but only when their expected retrieval gain justifies cost.

    \begin{itemize}
      \item Learn a per-query retrieval/rewrite router from offline rewards.
      \item Keep cost explicit through rewrite and retrieval-call penalties.
      \item Validate claims with baselines, bootstrap intervals, OPE, and artifact checks.
    \end{itemize}
  \end{alertblock}

  \begin{block}{Contextual-Bandit Formulation}
    Each query is a one-step decision:

    \[
      \pi(s) = \arg\max_a \mathbb{E}[R \mid s, a]
    \]

    \vspace{-0.5em}
    \[
      R = \mathrm{Recall@5} + 0.5 \cdot \mathrm{MRR}
          - \mathrm{RewriteCost} - \mathrm{ExtraRetrievalCallCost}
    \]

    \begin{itemize}
      \item \textbf{State:} query features + BM25 confidence statistics.
      \item \textbf{Actions:} BM25, dense, hybrid, keyword rewrite; Gemini actions are expensive extensions.
      \item \textbf{Policy:} direct-method action-value models with CV model selection.
      \item \textbf{Training evidence:} replayable full-information retrieval rewards; this is not online bandit learning, and selected-action replay is a diagnostic baseline.
    \end{itemize}
  \end{block}

  \begin{block}{Implementation Pipeline}
    \input{images/figure1}
  \end{block}

  \begin{block}{Policy Behavior}
    \input{images/figure3}
  \end{block}

  \begin{block}{Cross-Dataset Generalization}
    Evaluating policy transferability by testing trained routing policies on unseen target domains:
    \vspace{0.25em}
    \begin{table}
      \centering
      \small
      \begin{tabular}{l l r r r}
        \toprule
        \textbf{Source} & \textbf{Target} & \textbf{Policy Reward} & \textbf{Target Train-Best} & \textbf{Calls} \\
        \midrule
        SciFact & SciFact & 1.093 & 1.057 & 1.477 \\
        SciFact & NFCorpus & 0.386 & 0.377 & 1.597 \\
        NFCorpus & NFCorpus & 0.407 & 0.377 & 1.000 \\
        NFCorpus & SciFact & 1.064 & 1.057 & 1.000 \\
        \bottomrule
      \end{tabular}
    \end{table}
    \vspace{0.25em}
    \small The NFCorpus-trained policy transfers to SciFact with positive reward delta over target train-best. The SciFact-trained policy transfers to NFCorpus but incurs extra retrieval cost.
  \end{block}

\end{column}

\separatorcolumn

\begin{column}{\colwidth}

  \begin{block}{Datasets and Evidence Boundary}
    Final claims use seed-42 full-corpus BEIR runs with 600 train queries and 300 test queries per dataset: \textbf{SciFact} scientific-claim retrieval over 5,183 documents and \textbf{NFCorpus} biomedical/nutrition retrieval over 3,633 documents. Smoke tests verify code paths only.
  \end{block}

  \begin{block}{Main Results}
    \input{tables/table1}

    \vspace{0.35em}
    \input{images/figure2}

    The learned policy improves reward over the train-selected fixed action on both BEIR datasets, closing 22.6\% of the SciFact oracle gap and 34.4\% of the NFCorpus oracle gap. The constrained policy keeps positive utility gains while making call tradeoffs explicit.
  \end{block}

  \begin{block}{Diagnostics That Support the Claim}
    \begin{itemize}
      \item \textbf{Selected-action replay:} Linear Thompson delta is +0.005 on SciFact and +0.027 on NFCorpus.
      \item \textbf{Replay regret:} LinUCB selected-feedback regret is 82.1 vs 67.4 full-information direct on SciFact, and 43.2 vs 29.6 on NFCorpus.
      \item \textbf{OPE coverage warning:} SciFact IPS error rises from 0.127 under uniform logging to 0.268 under sparse train-best logging.
      \item \textbf{Three-seed robustness:} selective win rate is 0.667 on both datasets; mean deltas are +0.021 on SciFact and +0.025 on NFCorpus.
      \item \textbf{Audit trail:} markdown consistency, evidence consistency, checkpoint manifest, and artifact index pass.
    \end{itemize}
  \end{block}

  \begin{exampleblock}{Where the Result Gets Stronger}
    \input{tables/table2}

    \vspace{-0.4em}
    These checks turn the next-step analysis into evidence: the router is strongest on high-disagreement query slices, constrained routing can preserve utility near one call/query, and simple margin gating does not materially improve the main policy.
  \end{exampleblock}

  \begin{block}{Limitations and Claim Boundary}
    \begin{itemize}
      \item Evidence retrieval only; no generated-answer EM/F1 or LLM fine-tuning.
      \item Direct method uses replayable full-information action tables.
      \item Not online bandit learning; OPE is simulated, not real online logs; semantic features remain analysis-only.
    \end{itemize}
  \end{block}

\end{column}
\separatorcolumn

\end{columns}
\end{frame}

\end{document}
